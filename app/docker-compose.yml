services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8080:8000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
